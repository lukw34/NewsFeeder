import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    emptyListContainer: {
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 50
    },
    emptyListText: {
        marginTop: 50,
        fontFamily: 'Verdana',
        textAlign: 'center',
        fontSize: 20,
        color: 'white',
        width: 250
    }
});